name: flutter_medical_beauty
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  # 轮播图
  flutter_swiper_null_safety: ^1.0.2
  # 瀑布流
  flutter_staggered_grid_view: ^0.4.0
  # 加载网络图片
  cached_network_image: ^3.0.0
  # 网络请求
  dio: ^4.0.0
  # 刷新数据
  pull_to_refresh: ^2.0.0
  # 左滑删除
  flutter_slidable: ^0.6.0
  # 监听
  event_bus: ^2.0.0
  # 高德定位
  amap_flutter_location: ^2.0.0
  # 权限管理
  permission_handler: ^8.1.4+2
  # 用于读写简单键值对的Flutter插件。包装iOS上的NSUserDefaults和Android上的SharedPreferences。
  shared_preferences: ^2.0.6
  # 选择器
  flutter_picker: ^2.0.2
  # 图片选择器
  image_picker: ^0.8.3+2
  # 时间选择器
  flutter_datetime_picker: ^1.5.1
  # 时间格式转换
  date_format: ^2.0.4
  # 二维码生成
  qr_flutter: ^4.0.0
  # 保存图片
  image_gallery_saver: ^1.6.9
  # 加载Html
  flutter_html: ^2.1.1
  common_utils: ^2.0.2
  # 微信支付
  fluwx: ^3.5.1
  # 支付宝
  tobias: ^2.1.2+1
  # 浏览器
  webview_flutter: ^2.0.12
  # 打电话
  url_launcher: ^6.0.9
  # 路径
  path_provider: ^2.0.2
  # 多选图片
  multi_image_picker2: ^5.0.2
  # XInstall
  xinstall_flutter_plugin: ^1.5.1
  # 包信息
  package_info: ^2.0.2
  # 屏幕适配
  flutter_screenutil: 4.0.3+1
  # dialog
  flutter_smart_dialog: 1.2.6
  # 监听键盘弹起
  keyboard_visibility: 0.5.6
  # 常用工具类
  flustars: ^0.3.2
  # 推送 tpns
  tpns_flutter_plugin:
    git:
      url: https://github.com/TencentCloud/TPNS-Flutter-Plugin
      ref: V1.1.3
  #ease_call_kit
  ease_call_kit:
    git:
      url: https://github.com/easemob/ease_call_kit.git
      ref: stable
  # 环信 sdk
  im_flutter_sdk:
    path: ../im_flutter_sdk-stable
  # RTC Flutter SDK 视频通话
  ar_rtc_engine: ^4.2.0
  # 图片预览
  photo_view: ^0.13.0

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/tabs/
    - assets/images/home/
    #    - assets/images/category/
    - assets/images/profit/
    - assets/images/box/
    - assets/images/mine/
    - assets/images/video/
    - assets/resources/

    - images/logo.png
    - images/launch.jpg
    - images/login_bg.jpg
    - images/login_icon.png
    - images/login_pwd_show.png
    - images/login_pwd_hidden.png
    - images/login_usename_clear.png
    - images/login_protocal_selected.png
    - images/login_protocal_unselected.png

    - images/register_back.png

    - images/home_tab_chat.png
    - images/home_tab_contact.png
    - images/home_tab_me.png
    - images/home_tab_selected_chat.png
    - images/home_tab_selected_contact.png
    - images/home_tab_selected_me.png

    - images/chat_input_bar_close.png
    - images/chat_input_bar_emoji.png
    - images/chat_input_bar_keyboard.png
    - images/chat_input_bar_more_show.png
    - images/chat_input_bar_more_close.png
    - images/chat_input_bar_voice_hidden.png
    - images/chat_input_bar_voice_show.png
    - images/chat_input_more_call.png
    - images/chat_input_more_camera.png
    - images/chat_input_more_file.png
    - images/chat_input_more_loc.png
    - images/chat_input_more_photo.png
    - images/chat_input_more_video.png
    - images/chat_input_more_pin.png

    - images/chat_bubble_img_broken.png
    - images/chat_bubble_voice_ripple00.png
    - images/chat_bubble_voice_ripple01.png
    - images/chat_bubble_voice_ripple02.png

    - images/chat_faces_delete.png

    - images/contact_default_avatar.png
    - images/contact_select_check.png
    - images/contact_select_uncheck.png

    - images/expression/
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware.

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
